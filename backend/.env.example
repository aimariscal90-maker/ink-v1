# Backend environment variables for development
# CORS - comma separated list of allowed origins. Use '*' to allow all.
# Examples:
# - For local development with Vite: ALLOWED_ORIGINS="http://localhost:5173"
# - For Codespaces (replace <CODESPACE> with the workspace alias):
#   ALLOWED_ORIGINS="https://<CODESPACE>-5173.app.github.dev,https://<CODESPACE>-8000.app.github.dev"
# - For development quick-start (not recommended for production): ALLOWED_ORIGINS="*"
ALLOWED_ORIGINS="*"
ALLOW_CREDENTIALS=false

# Settings
ENVIRONMENT=development

# Optional API keys (add in production only)
# OPENAI_API_KEY=
# GOOGLE_PROJECT_ID=
# GOOGLE_CREDENTIALS_FILE=
